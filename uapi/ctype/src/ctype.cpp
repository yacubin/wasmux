/*
 *
 *  Copyright (C) 2024-2025  Yurii Yakubin (yurii.yakubin@gmail.com)
 *
 */

#include "config.h"

#include <ctype.h>
#include <assert.h>

int toupper(int ch)
{
  assert(0);
  return ch;
}

int tolower(int ch)
{
  assert(0);
  return ch;
}

int isalnum(int ch)
{
  assert(0);
  return 0;
}

int isalpha(int ch)
{
  assert(0);
  return 0;
}

int iscntrl(int ch)
{
  assert(0);
  return 0;
}

int isdigit(int ch)
{
  assert(0);
  return 0;
}

int isgraph(int ch)
{
  assert(0);
  return 0;
}

int islower(int ch)
{
  assert(0);
  return 0;
}

int isprint(int ch)
{
  assert(0);
  return 0;
}

int ispunct(int ch)
{
  assert(0);
  return 0;
}

int isspace(int ch)
{
  assert(0);
  return 0;
}

int isupper(int ch)
{
  assert(0);
  return 0;
}

int isxdigit(int ch)
{
  assert(0);
  return 0;
}

int isascii(int ch)
{
  assert(0);
  return 0;
}

int isblank(int ch)
{
  assert(0);
  return 0;
}
